// По ТЗ анимированный header не потребовался

//  window.onscroll = function() {
//  const hero = document.querySelector('.hero-nav');
//  const heroHeight = hero.offsetHeight;
//  const header = document.querySelector('.header');
//  const headerHeight = header.offsetHeight;
//  const scrollOffset = window.scrollY;
//
//  if (scrollOffset < heroHeight) {
//    hero.style.height = (heroHeight - scrollOffset) + 'px';
//    header.style.height = (headerHeight - scrollOffset) + 'px';
//  }
//
//  if (scrollOffset > (heroHeight - 215)) {
//    hero.classList.add('fixme');
//  } else {
//    hero.classList.remove('fixme');
//  }
//};

